import { brandName } from "@/constants/common";
import Head from "next/head";
import { FC } from "react";
import Image from 'next/image';
import SidebarMenu from "./SidebarMenu";
import { useRecoilValue } from "recoil";
import { userAtom } from "@/states/atom";

type PrivateLayoutProps = {
    title?: string;
    children?: JSX.Element;
    backgroundColor?: string;
}

const ChooseSpotsLayout: FC<PrivateLayoutProps> = ({ children, title, backgroundColor = "" }) => {
    const user = useRecoilValue(userAtom);

    return (
        <>
            <Head>
                <title>{brandName}</title>
                <meta name="description" content="Generated by create next app" />
                <meta name="viewport" content="width=device-width, initial-scale=1" />
                <link rel="icon" href="/favicon.ico" />
            </Head>
            <div className="d-flex">
                <SidebarMenu flow="user" />
                <div style={{
                       background: `url(/gradient-blue-bg.png) no-repeat`,
                       backgroundSize: '100%',
                       position: 'fixed',
                       overflowY: 'scroll',
                       top: '0px',
                       bottom: 0,
                       backgroundAttachment: 'scroll',
                       right: '0px',
                       left: '250px',
                       padding: '46px 46px 46px 0',
                       minHeight: '1000px'
                }}>
                    <div className="d-flex justify-content-between">
                        {!!title?.length ? <p className="text-white" style={{paddingLeft: '46px'}}>{title}</p> : <div />}
                        <div className="d-flex align-items-center gap-4">
                            <div className="d-flex align-items-start gap-2 text-center text-decoration-none">
                                <Image
                                    priority
                                    src="/icons/user.svg"
                                    alt="Follow us on Twitter"
                                    style={{
                                    }}
                                    height={25}
                                    width={25}
                                />
                                <p className="text-white">{user?.name}</p>
                            </div>
                        </div>
                    </div>
                    <div style={{ backgroundColor }}>
                        {children}
                    </div>
                </div>
            </div>
        </>
    );
};

export default ChooseSpotsLayout;
